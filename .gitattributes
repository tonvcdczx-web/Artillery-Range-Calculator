# Auto detect text files and perform LF normalization
* text=auto
import streamlit as st

# 1. ฟังก์ชันคำนวณยังคงเดิม
def calculate(num1, num2):
    # สูตรคำนวณ: (num1 * 0.011303) + num2 + 0.2
    result = (num1 * 0.011303) + num2 + 0.2
    return result

# 2. ฟังก์ชัน main ถูกแปลงเป็นโครงสร้าง Streamlit
def main():
    # กำหนดหัวข้อและคำอธิบาย
    st.title("เครื่องคำนวณระยะยิงปืนใหญ่ 5 นิ้ว")
    st.markdown("---") # เส้นคั่น

    # 3. ใช้ st.number_input สำหรับรับค่าตัวเลข (แทน input())
    # กำหนดค่าเริ่มต้นเป็น 0.0 เพื่อป้องกัน error
    
    num1 = st.number_input(
        "กรอกตัวเลขระยะทาง (Num1):", 
        value=0.0, 
        step=1.0, # ให้เพิ่ม/ลดทีละ 1
        format="%.2f", # แสดงทศนิยม 2 ตำแหน่ง
        help="ระยะทางเป้าหมาย (หน่วยในเกม)"
    )
    
    num2 = st.number_input(
        "กรอกตัวเลขตัวความต่างของพื้น (Num2):", 
        value=0.0, 
        step=0.1, # ตัวเลขความต่างมักจะเป็นทศนิยม
        format="%.4f", # แสดงทศนิยม 4 ตำแหน่ง
        help="ใส่ค่าบวก (+) ถ้าพื้นสูงกว่าปืน, ใส่ค่าลบ (-) ถ้าพื้นต่ำกว่าปืน"
    )

    # 4. ปุ่มสำหรับกดคำนวณ
    if st.button("คำนวณผลลัพธ์"):
        # เรียกฟังก์ชันคำนวณ
        result = calculate(num1, num2)
        
        # 5. ใช้ st.success หรือ st.write สำหรับแสดงผล (แทน print())
        st.success(f"💥 ผลลัพธ์คือ: **{result:.4f}**")
        st.write("นี่คือค่าที่คุณต้องปรับในการยิงปืนใหญ่!")

# เรียกใช้ฟังก์ชัน main เมื่อสคริปต์รัน
if __name__ == "__main__":
    main()